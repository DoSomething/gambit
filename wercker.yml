box: node:8.11.1
services:
  - id: mongo
build:
  steps:
    - npm-install
    - script:
      name: run all tests
      code: npm run test:full
    - script:
      name: publish coverage report to codecov
      code: npm run publish-coverage-report
